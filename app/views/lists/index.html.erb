<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path('central.jpg') %>)">
  Bruna's beautiful movies lists
</div>


  <div class="container mt-4 d-flex justify-content-end">
    <%= render 'form', list: @list %>
  </div>

<div class="container">
  <h3 class="mt-1">My lists of favorites</h3>
  <hr>
  <div class="row">
    <% @lists.each do |list| %>
      <div class="col-lg-3 col-md-4 col-sm-6 mb-4 mt-1">
        <div class="card h-100 d-flex justify-content-center">
          <div class="card-body d-flex flex-column align-items-center">
            <%= link_to list.name, list_path(list.id), class: 'm-2 text-decoration-none' %>
            <%= link_to "Delete", list_path(list), class: 'btn btn-sm btn-outline-danger my-2', data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this list?" } %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
